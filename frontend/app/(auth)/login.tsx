import { View, Text } from "react-native";

export default function LoginScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-2xl font-bold text-foreground">☕ SekoPi</Text>
      <Text className="text-muted-foreground mt-2">Login page — coming soon</Text>
    </View>
  );
}
