server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Beritahu app bahwa request aslinya HTTPS
    add_header X-Forwarded-Proto https;
    add_header X-Forwarded-For $proxy_add_x_forwarded_for;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
